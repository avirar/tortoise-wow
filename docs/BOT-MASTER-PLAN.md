<!-- Canonical copy: /root/bot-master-plan.md — edits there, then re-sync. Synced 2026-09-20 (overnight commit 54feac8f: L6 ACSOAP realtime transport + GO/chest looting [VisitGridObjects fix] + level-1 regen + idle-sweep removal). -->

**Status:** DRAFT — documentation phase, no implementation started yet
**Date:** 2026-06-27
**Supersedes:** the scope limits in `playerbot-port-plan.md` (that plan's engine-porting detail remains valid history)
**Owner intent (verbatim):** *"I think it's time to remove that limit. I want the best possible bots for tortoise! I'm not precious about which bot system we pull our solutions from — I strongly believe in diversity of sources at all times."*

---

## 1. Vision

Build the best possible bot system for Tortoise-WoW (1.18.1 / 7272) by treating every existing bot codebase as a **code mine**, not a framework to adopt wholesale. Per-subsystem, take the best implementation available from any source, adapted onto our chassis.

**The chassis is non-negotiable:** the AC-mod-playerbots decision engine (3-engine shared context, triggers/strategies/actions/values, NamedObjectContext factories) already ported to `src/game/PlayerBots/` and independently verified correct by the 2026-06-26 critical review (`playerbot-review.md`). Everything else is liftable, replaceable, or deletable.

## 2. Source inventory

| Source | Location | Lineage | What we take from it |
|---|---|---|---|
| **Our AC port** | `/root/tortoise-wow` branch `playerbot-engine-port` (HEAD `c5f57bd`, pushed to avirar) | AzerothCore mod-playerbots | **Chassis**: Engine, Queue, contexts, login/persistence/factory, loot pipeline, packet triggers, item stat engine |
| **Shyalya fork** | git remote `shyalya` in `/root/tortoise-wow` (full 903-commit history fetched 2026-06-27, +52MB) + browsable mirror `/root/shyalya-tortoise-wow` — ⚠ **upstream archived read-only 2026-09-30** | CMaNGOS ike3 (the ancestor of AC's system) | Features to mine: BG tactics (Arathi node capture), AHBot + market policy, taxi/travel system (~15 fixes), ManTech scheduling for 6k bots, diagnostics suite, 5,082-line config surface |
| **AC mod-playerbots reference** | `/root/azerothcore-wotlk/modules/mod-playerbots/` | AC (actively maintained) | Per-class strategies for all 9 classes, ItemUsage semantics, group/dungeon behaviors, quest/travel strategies, raid scripts |
| **mod-ollama-bot-buddy** | `/root/azerothcore-wotlk/modules/mod-ollama-bot-buddy/` | AC module (user's own) | Agent↔bot interface logic: command API, game-state snapshots, chat override (see `agent-bot-interface-plan.md`) |
| **acore-data** | `/root/acore-data` (+ client data at `/root/azerothcore-wotlk/env/dist/bin`, wowgaming v20.0) | AC tooling (user's own) | **Concepts only** → new build `tortoise-data`, a tortoise-native CLI driven by mangos `DBCfmt` strings (see `tortoise-data-plan.md` v2 — fork rejected: AC registry machinery was the cost driver and doesn't apply) |

Key lineage fact: AC mod-playerbots **descends from** ike3/CMaNGOS bots, so Shyalya's module shares conceptual DNA (actions/triggers/strategies/values) with our chassis despite different internals (`ReactionEngine`, `Multiplier`, `ActionBasket`, per-class strategy dirs). Concepts translate even when code doesn't.

## 3. Architecture decisions

| # | Decision | Rationale |
|---|---|---|
| A1 | **Stay in-core** (`src/game/PlayerBots/`) — no module infra | Our base (Penqle main @ `31586bd`) predates the module system Shyalya's tree has. Porting the module loader for one consumer is churn; mining feature logic into our tree is cheaper. Revisit only if we adopt many modules. |
| A2 | **Base sync first** — rebase/merge `playerbot-engine-port` onto current Penqle `main` (we are 251 commits behind) | Every Shyalya lift assumes a newer core (their bot commits touch `World.cpp`, `WorldSession.cpp`, `CharacterHandler.cpp`, `MovementHandler.cpp`, `AuctionHouseHandler.cpp`). Syncing gives us those files' current shape and 251 commits of upstream fixes (quest fixes, vmap guard, movespline clamp, Windows build fixes). |
| A3 | **Class AI = AC per-class strategies**, with `SelectOffensiveSpell` kept as universal baseline | Review confirmed the bridge is now wired (commit `435d129`): all 9 classes cast via `cast spell` default action. AC class strategies layer priorities/rotations on top per class — port them class-by-class like the Warrior pattern. |
| A4 | **Item evaluation = our vanilla stat engine** (now wired, commit `e7603c0`) | Ours is vanilla-correct (raw % hit/crit, ON_EQUIP spell stats, spec-aware weights). AC's `ItemUsageValue` semantics (need/greed/sell/quest decisions) port **on top** of our scorer. |
| A5 | **Shyalya lifts are rewrites onto our chassis**, with source-file citations in commit messages | ike3 code won't drop in (different engine internals, different API names). We port the *logic* (e.g., AB banner capture state machine) as AC-style triggers/actions. |
| A6 | **Diversity-of-sources policy**: every subsystem cites its origin; when two sources solve the same problem, prefer (1) correctness on vanilla 1.12-era mechanics, (2) maintainability on our chassis, (3) feature depth | Explicit user policy. Avoids "not invented here" and "not re-invented here" failure modes. |

## 4. Review reconciliation — `playerbot-review.md` vs HEAD `c5f57bd`

The local-AI review (2026-06-26) was written **before** the 2026-06-27 commit batch. Status at current HEAD:

| Finding | Severity | Status | Where |
|---|---|---|---|
| P0-1 `cast spell` dead code → 8/9 classes never cast | P0 | **FIXED** — registered in `ActionContext.cpp:31`, default action in `CombatStrategy.cpp:15`; Melee/Ranged strategies per class in `PlayerbotAIBase::Initialize` | `435d129` |
| P0-2 `|| true` forces melee auto-attack for ranged | P0 | **FIXED** — `shouldMelee = CanReachWithMeleeAutoAttack \|\| melee-class check` | `435d129` |
| P0-3 StatsWeightCalculator 100% dead code | P0 | **FIXED** — wired into `ItemUsageValue.cpp:71` (plus spec-aware weights, on-hit/on-use spell stats) | `e7603c0` |
| P1-1 Warrior `GetSpecDefaultActions` dead | P1 | **FIXED** — consumed by `GenericWarriorStrategy::getDefaultActions` | `435d129` |
| P1-2 free-repair exploit + no spirit-healer path | P1 | **FIXED** — `DurabilityRepairAll` calls removed; `dead → spirit healer` trigger in `DeadStrategy` | `435d129` |
| P2-1 iteration cap computed once | P2 | **FIXED** — AC refresh-per-iteration pattern | `435d129` |
| P2-2 redundant `SetTargetGuid` | P2 | **FIXED** — removed (SetSelectionGuid does both) | `435d129` |
| P1-3 Warriors hardcoded to Arms; no spec detection | P1 | **FIXED 2026-09-16** — `Util/SpecDetect.h` (shared talent-tab detection, StatsWeightCalculator now delegates); Arms/Fury/Tank chosen by dominant tree at login | pending commit |
| P2-3 `StoreLootAction` mutates `is_looted` directly | P2 | **FIXED 2026-09-16** — direct write removed; opcode handler marks looted server-side (`LootHandler.cpp:225-249`) | pending commit |
| P2-5 `GetName()` (`std::string`) passed to `%s` | P2 | **NON-ISSUE** — tortoise `Player::GetName()` returns `char const*` (`Player.h:2758`) | — |
| P2-6 FFA loot "inverted" | P2 | **FIXED 2026-09-16** — review read wrong: AC also skips FFA by default, gated by config; ported AC semantics (`PlayerBot.FreeMethodLoot=1` opts in) | pending commit |
| P3 batch (static map leak, dead `lastRelevance`, RepopAction tautology) | P3 | **PARTIAL 2026-09-16** — static-map leak fixed (prune >10min entries); stale-target race verified safe (runs before actions); `lastRelevance` + `release` action left (low risk, revisit in R2) | pending commit |

## 5. Roadmap (scope limits removed)

Phases are ordered by dependency, not priority — R4/R6 can interleave once R1 lands.

### R0 — Tooling & docs (this phase, docs only)
- [x] `bot-master-plan.md` (this file)
- [x] `tortoise-data` — **IMPLEMENTED** (v0.1: `tw` CLI at /root/tortoise-data, pushed to github.com/avirar/tortoise-data; DBCfmt-driven DBC reader + read-only SQL, tortoise spell_table/WorldSafeLocs specifics handled)
- [x] `agent-bot-interface-plan.md` — bot-buddy logic port
- [x] Websearch/fetch — RESOLVED via `pi-web-access` package (Exa zero-config fallback chain), custom SearXNG extension spec not needed
- [x] AC client data installed (wowgaming v20.0 → `env/dist/bin`) for acore-data reference runs
- [ ] Update `AGENTS.md` hub, supersede port-plan scope, commit docs

### R1 — Base sync & stabilization (IN PROGRESS — see `r1-base-sync-log.md` for issues & solutions)
- [x] Merge of Penqle `main` onto `playerbot-engine-port` (merge `a473d4f5`; backup branch `playerbot-engine-port-pre-r1`)
- [x] Build fix: WorldSession bot plumbing re-added (`GetBot/SetBot/m_bot`, friend for private `LoginPlayer`)
- [x] DB auto-updater fixed (config key rename + duplicate-key removal; 146 world + 1 char migration reconciled idempotently)
- [x] mangosd boots on new base; 100 bots queued at startup
- [x] **100/100 bots online** — bot login via upstream `HeadlessSessionMgr` (socketless sessions can't live in `World::m_sessions`; `UpdateSessions` purges non-connected sessions) — see `r1-base-sync-log.md` §5
- [x] Fix open review items: P1-3 spec wiring, P2-3, P2-6, P2-5 audit, P3 cleanup (2026-09-16; commits `ceeb728c`, `97ab55cc`)
- [x] **CRITICAL (2026-09-16):** merge also dropped `sPlayerBotMgr.OnPlayerInWorld(this)` from `Player::AddToWorld` → bots online but AI never ran (engines 0/0/0, no movement/casts). Re-added (+ include) — commit `24512ec1`; verified active (engines running, movement/targeting/casts). This invalidated all pre-fix "soaks" — soak clock restarted with AI verified (`r1-base-sync-log.md` §6b)
- [x] Also restored bot pet-taming guard in `Spell.cpp` dropped by merge (commit `f273bb10`)
- [~] Bot login soak ≥30 min with AI active on new base — restarted 2026-09-17 08:45 with spell fix deployed; 100/100 online, 0 real crashes, all 9 classes casting. (Soak clock running.)
- **Exit criteria:** soak passes on new base; review table all-green.

### R2 — Class parity (AC strategies, 8 classes)
- **STATUS (2026-09-17): essentially complete.** 7/9 classes have dedicated combat strategies (Warrior Arms/Fury/Tank full, Mage v1, Warlock, Priest, Shaman, Paladin, Druid). Rogue/Hunter intentionally on the generic fallback (no low-level class CC; offensive table covers their rotation) — deferred until higher-level features. Full warrior spec trees now include all triggers. Verified live: all 9 classes learning + casting class spells, `fear`/`heal self`/polymorph/stances firing, 100/100 online, 0 crashes.
- Port AC per-class strategy trees following the established Warrior pattern (`Ai/Class/<Class>/`): rogue, priest, mage, warlock, hunter, shaman, paladin, druid.
- [x] **Prerequisite: spell auto-learn fixed (2026-09-17, commit `3e5932cf`)** — factory-created bots lack base class skills and `AutoLearnSpellsForLevel`'s `req_skill_value != 0` filter skipped every `skill_line_ability` row, so non-warrior bots had 0 class spells. Now trains the skill to the spell's required value then `LearnSpell`. All 9 classes learn + cast their spells in combat. Without this, no class strategy can function.
- [x] **Class enum is non-vanilla** (`ChrClasses.dbc`): 7=Shaman, 8=Mage, 9=Warlock, 11=Druid. `characters.class` and `skill_line_ability.class_mask` are consistent via `GetClassMask()=1<<(c-1)` — use `tw dbc ChrClasses` for ground truth, never assume vanilla numbering.
- [x] Spec detection: talent-tab detection in `Util/SpecDetect.h`, warrior spec strategy selected at login (P1-3, 2026-09-16) — extend to other classes as their strategy trees land
- [x] **Mage v1 (2026-09-17, commit `3e5932cf`)**: all-spec `GenericMageStrategy` — Polymorph CC action + `can polymorph` trigger, cast-spell nukes via `SelectOffensiveSpell` per-class table, inherits `RangedCombatStrategy` flee-when-close. Blink deferred (positioning risk).
- [x] **5 more class strategies (2026-09-17)**: `GenericWarlockStrategy` (Fear CC), `GenericPriestStrategy` / `GenericShamanStrategy` / `GenericDruidStrategy` (combat self-heal), `GenericPaladinStrategy` (Hammer of Justice CC + self-heal). Shared `SelfHealAction` (`Ai/Base/Actions/`) resolves the per-class heal spell and casts on self, gated by the `low health` trigger (≤30%) on the COMBAT engine (no conflict with the NON_COMBAT food system). Verified live: `fear` + `heal self` actions firing, 100/100 online, 0 crashes.
- [x] **Learnable-spell lesson (2026-09-17)**: CC/self-heal spells must be ones the bots actually learn — `AutoLearnSpellsForLevel` only grants spells present in `skill_line_ability` (by `class_mask`). **Hex (11641) is `spellLevel=30` and ABSENT from `skill_line_ability`** → never learned → warlock CC uses **Fear (5782)** instead. Self-heal base IDs had to match `character_spell` ground truth: Priest **2053** (not 2060), Shaman **332** (not 331), Paladin **639** (not 635), Druid **5186 Healing Touch** (not 3734 Regrowth). `GetHighestKnownSpell(base)` walks the whole chain, so any ID in the chain works.
- [ ] Rogue/Hunter: rely on the class offensive table (Eviscerate/Sinister Strike/Garrote; Auto Shot/Arcane Shot/Serpent Sting) — no low-level class CC to add; generic `Melee`/`Ranged` fallback. Defer dedicated trees until higher-level (poison/aspect) features matter.
- [x] **Warrior: 6 missing Fury/Tank triggers (2026-09-17)** — `bloodthirst ready`, `whirlwind ready`, `shield slam ready`, `revenge ready`, `taunt needed`, `demoralizing shout needed` now implemented in `WarriorTriggers.{h,cpp}` and registered in `TriggerContext.cpp`. Use `FindSpellIdByName` (returns 0 when the bot lacks the spell) so they are harmless dead branches until a bot actually learns the ability (Bloodthirst 23880 / Shield Slam 23922 are talent spells absent from `skill_line_ability`; Whirlwind/Revenge/Taunt/Demoralizing Shout are class spells). Taunt gates on `target->GetVictim() != bot` (aggro lost); Demoralizing Shout re-applies only when the target lacks the debuff (`TargetHasAuraFromChain(target, 1160)`).
- Per-class verification: rotation logs show ability usage per class at level-appropriate targets.
- **Sources:** AC `strategy/<class>/`; cross-check spell availability tables against `tw_world` via tortoise-data (R0 tool) and `twow-class-spells-reference.md`.

### R3 — Item & loot pipeline hardening
- **Loot-pipeline investigation (2026-09-17)**: The "open loot FAILED 98%" is largely the **benign re-entry guard** — `OpenLootAction` returns false while a loot window is already open (`GetLootGuid()` set) waiting for `StoreLootAction`; `StoreLootAction` correctly clears the loot guid + loot target on every exit path (processed/not-tapped/empty) and replicates AC's `lootslot_type` permission logic. The code is sound; the pipeline is **content-limited**, not buggy.
- **"No gear"/"no gold" root cause (→ R5) — diagnosed + fixed (2026-09-17):** bots accumulate **0 gold** and little gear because they fight **beasts** (Timber Wolf/Rabbit/etc. have `gold_min=gold_max=0` and drop no equipment) instead of **humanoids** (gnolls/forsaken drop gold, e.g. Amberpaw Gnoll 41-59g, + cloth/leather). Verified: the `CMSG_LOOT_MONEY` → `HandleLootMoneyOpcode` → `LootMoney()` path is correct (ungrouped bots take all gold), `GenerateMoneyLoot` uses DB `creature_template.gold_min/max` (loaded via `CreatureInfo`), `Rate.Drop.Money`=1.0. Bots DO loot (3,509 items held, incl. humanoid cloth) but loot is mostly beast drops (jerky/meat/fang/pelt). **Why beasts win:** `SelectNearestSafeTarget` applies a humanoid distance-weight *after* a **mob-contention filter** (skip creatures with `GetVictim()!=bot` or tapped `HasLootRecipient()&&!IsTappedBy`) — with **300 bots** in finite-mob zones most creatures are contested and filtered, leaving a sparse set where beasts often dominate; the 0.6 weight only applies to survivors so it can't overcome contention. Zones actually have **2x more humanoid than beast spawns** (Elwynn 2488 hum/1202 beast; high-level 8170/4020). **Fix (deployed 2026-09-17):** replaced the weight with a **two-tier preference** — `PlayerBot.HumanoidPreferRange` (default now **150yd** = full sight): target the NEAREST non-contested humanoid within range (gold/gear source), else the nearest target of any type. (Initial 12yd too small; 30yd still lost humanoids at 58+yd to nearer beasts → raised to full sight.) **Post-deploy verification (2026-09-17):** humanoid `best=` picks confirmed working, money path verified end-to-end — but the kill distribution remains ~90% zero-gold creatures (beasts + the 0-gold Mudpaw "humanoids"), so gold accumulation is still thin (see R5 loot-money bullet for the full analysis and next levers).
- Full AC `ItemUsage` semantics on top of our scorer: need/greed/pass group decisions, sell gray, quest items, bag slots, consumables.
- Group loot roles (ML distribution, roll participation) — extend the `lootslot_type` work.
- Vendor/sell cycles + gold accumulation for bots.
- **Sources:** AC `strategy/values/ItemUsageValue.cpp`, `strategy/actions/UseItemAction.cpp`; Shyalya ahbot as reference for economy sanity.

### R3a — Item scoring & usage pipeline (assessment 2026-09-18)

**Finding: all three "port candidates" already exist in-tree as a vanilla-adapted v1** (commits `b8ab94e7` loot system compiled, `e7603c03` "Item evaluation: vanilla percentage stats, spec-aware weights", `ceeb728c` shared spec detection). They were built during the P1-3/P3-4 review-fix era, adapted to vanilla, and are **largely dead code** — nothing executes the decisions yet.

| Component | Where | State |
|---|---|---|
| `StatsCollector` (315ln) | `PlayerBots/Mgr/Item/` | v1 complete (DPS/armor/block/7 base stats/item spells by trigger/aura map) but with **real bugs** (below) |
| `StatsWeightCalculator` (596ln) | `PlayerBots/Mgr/Item/` | Spec tables for all 9 classes × 3 tabs (vanilla tab map), role detect, armor-type + quality multipliers; **missing** overflow/set/weapon-penalty/enchant/slot-awareness |
| `ItemUsageValue` (114ln) | `PlayerBots/Ai/Base/Value/` | `QueryItemUsageForEquip` fully implemented (empty-slot → EQUIP, else REPLACE at `equipUpgradeThreshold` 1.1) and registered as the "item usage" AI value — **but `Calculate()` always returns NONE and NO trigger/action consumes the value; no `UseItemAction` exists** |
| `EquipUpgradesAction` | `Ai/Base/Actions/LootAction.cpp` | Still uses the crude `ItemLevel × (Quality+1)` score — the calculator is **not wired in** |
| `RandomSuffixCache` (new, P1) | `PlayerBots/Mgr/Item/` | One-time startup SQL cache of `item_enchantment_template` (781 suffix classes / 27,687 rows) — chance-weighted class averages + instance-accurate lookup via `sItemRandomPropertiesStore` |
| Config | `PlayerbotAIConfig` | `equipUpgradeThreshold` (1.1) already present; `debugScoreDump` (P1 verification hook, default OFF) added |

**Vanilla-vs-WotLK data-model lessons (verified 2026-09-18, `tw` + core source):**
- **Only 7 base item-stat types exist in the DBC array** (`ITEM_MOD_MANA/HEALTH/AGI/STRA/INTE/SPRI/STAM`, `MAX_ITEM_MOD=8`); core `_ApplyItemBonuses` ignores stat types ≥8 (no `default:` case). **Hit/crit/haste/defense/dodge/parry/block/SP/heal-power/mana-regen come from ITEM SPELLS** (`proto->Spells[]` auras) — so the collector's `CollectSpellStats` path is the *primary* stat source, not an optimization. (WotLK put everything in the `Stats[]` array — the AC collector shape does not map 1:1.)
- **Flat % semantics**: `SPELL_AURA_MOD_HIT_CHANCE` (54), `MOD_SPELL_HIT_CHANCE` (55), `MOD_ATTACKER_RANGED_HIT_CHANCE` (185), `MOD_CRIT_PERCENT`, etc. are **raw percentage points** (no rating division). The existing collector's `×0.02` factors on hit/crit are a WotLK-rating leftover — **bug** (under-credits by 50×).
- **`SPELL_AURA_MOD_STAT`**: `EffectMiscValue` = stat index (0=STRA, 1=AGI, 2=INTE, 3=SPRI, 4=STAM; <0 = all stats) — core `Aura::HandleAuraModStat` (SpellAuras.cpp:4621) confirms. The existing collector dumps MOD_STAT into `BONUS ×0.5` — **main stats on cloth gear are lost**.
- **Green suffixes = SQL, not DBC**: `item_template.RandomProperty` → `GetItemEnchantMod()` rolls the weighted `item_enchantment_template` table (`entry/ench/chance`, 27,687 rows; in-memory `RandomItemEnch` map). The `sItemRandomPropertiesStore` DBC only holds `enchant_id[3]` per suffix. **Collector's `CollectRandomSuffix` is a no-op → every green item loses its suffix stats.**
- **No sockets in this data** (no columns in `item_template`, no fields in `ItemPrototype`) → socket/gem scoring is a correct no-op here (unlike WotLK).
- **Talent tabs**: `TalentEntry.TalentTab` (0/1/2 per class) — our `SpecDetect.h` already has the vanilla map (e.g. Warrior 0=Arms/1=Fury/2=Protection; differs from AC's enum order → any AC weight-table port must re-map tabs).
- **No DK, no expertise/armor-pen/resilience, no spell penetration** in vanilla item stats (enum already correct: no such entries).
- **Item sets**: `Player::m_ItemSetEff` (`ItemSetEffect{setid,item_count,spells[8]}`) + `sItemSetStore` (`ItemSetEntry.spells[8]`) both exist → set-bonus scoring is portable.
- **Overflow API**: `Unit::GetTotalAuraModifier(AuraType)` (+`ByMiscValue`) exist → flat-% overflow penalties are trivial in vanilla (melee 3% / ranged 3% / spell ~16% caps; the existing `HitCap` constants exist but are unused).

**Phases (docs-first; implement in this order):**
- **P1 — Collector correctness (~200 lines in `StatsCollector.cpp`):** (1) drop the `×0.02` hit/crit factors (flat %); (2) add `MOD_SPELL_HIT_CHANCE` + `MOD_ATTACKER_RANGED_HIT_CHANCE` with collector-type routing (caster→spell hit, ranged→ranged, melee→melee; cross-credit at low weight); (3) `MOD_STAT` via `EffectMiscValue` → real stats (incl. <0 = all 5); (4) implement `CollectRandomSuffix` via `item_enchantment_template` (startup cache of entry→{ench,chance}, **chance-weighted average** — instances roll randomly, so the honest expected value is the mean, not AC's optimistic "best suffix"; cache like the hub/POI caches, one-time query); (5) unknown auras/effects → 0 credit instead of `BONUS` inflation; (6) verify aura names against `SpellAuraDefines.h` at build time (compile-time constants). Verify: score a known lvl-10 cloth chest (intel/spirit via item spell) + a green helm and sanity-check the numbers by hand against DBC/DBC-table values.
- **P2 — Calculator completion (~300 lines in `StatsWeightCalculator.cpp`):** (1) overflow penalties from `GetTotalAuraModifier` (melee/ranged/spell hit by collector type; `validPoints = max(0, cap − current)`); (2) item-set bonus (equip-set delta via `ItemSetEff` → set spells → collector); (3) weapon-type penalty (port AC `CalculateItemTypePenalty` vanilla-cut: 2H-in-1H-slot, shield, DW mismatch via `Player::CanDualWield()`, dagger 1.5× for rogue, wrong weapon class for hunter); (4) `CalculateItem(itemId, randomPropertyId, slot)` signature — slot-aware so P2.3 works (AC's `preferredSpecWeapons` speed tuning = optional follow-up, config-gated, default OFF). Verify: scores of representative gear across classes/spots (e.g. lvl-40 warrior: 2H vs 1H+shield, plate vs mail) + a logged score dump command or LOG_DEBUG sample.
- **P3 — Item-usage wiring (~300 lines):** (1) extract `QueryItemUsageForEquip` logic into a shared helper (used by both `ItemUsageValue` and the action — kill the duplicated path); (2) rewrite `EquipUpgradesAction` to use it (replaces ilvl×quality scoring) — this is the money feature: bots will finally pick green/blue stat gear over gray ilvl; (3) consumables: keep the existing food/drink system, add potion/bandage use (vanilla: `ITEM_SUBCLASS_BANDAGE=7`, on-use heal/energize items) with stack caps when `PlayerBot.UseConsumables=1` (default ON) — HP/MP thresholds reuse the existing health/mana values; (4) junk policy: non-equip, sellPrice 0, no usable effect → `Destroy` (config `PlayerBot.JunkDestroy=0` default OFF until the vendor cycle exists — bag bloat is the failure mode, AC keeps items but we have no vendor); (5) `ITEM_USAGE_VENDOR` stays a decision-only sentinel (vendor selling = R3 economy cycle, separate workstream). Verify: 10-bot soak with loot-rich humanoid spawns; check `rndbot stats`-level action counters for "equip upgrades" success rate (was 100% fail pre-R5), bag-item counts not growing unboundedly, and spot-check 5 bots' gear vs score expectations.

**P1 — Collector correctness (2026-09-18, DONE + verified live):**
- All six P1 items implemented: flat-% hit/crit (dropped `×0.02`), spell/ranged hit with collector-type routing, `MOD_STAT` via `EffectMiscValue` (incl. <0 = all 5), `CollectRandomSuffix` (chance-weighted class average) + `CollectRandomSuffixInstance` (instance-accurate for already-rolled greens via `sItemRandomPropertiesStore`), 0-credit for unknown auras/effects/item-stat types (no BONUS inflation).
- **New data-model lessons from live verification (2026-09-18):** (1) **Spells load from SQL `tw_world.spell_template` (150 cols, positional `SELECT *`), NOT DBC** — the `spell` table is an empty shell; lowercase cols (`entry`, `effect1`, `effectApplyAuraName1`, `effectBasePoints1`, `effectMiscValue1`, `effectDieSides1`). (2) **This core's `ItemSpelltriggerType` is WotLK-style: ON_USE=0, ON_EQUIP=1, CHANCE_ON_HIT=2** — data matches (trigger 0 = consumables 5078; 1 = passive gear 6155 of which 5290 armor; 2 = on-hit 316 all weapons); engine applies passive spells ONLY at trigger==1 (`Player::ApplyItemEquipSpell`). (3) **`ITEM_CLASS_ARMOR=4`** (custom enum, GEM=3) — not 5. (4) **`SubClass` = ARMOR TYPE for armor items (1=cloth/2=leather/3=mail/4=plate), NOT slot** — slot is `InventoryType`. (5) **Aura handlers 46/48/50 (dodge/parry/block *_SKILL) are `HandleUnused`** — only percent variants 47/49/51 are live; generic 52/54 apply to BOTH melee and ranged (engine adds both). (6) **Aura 71 = MOD_SPELL_CRIT_CHANCE_SCHOOL** (482 items). (7) **Haste auras 138/140/141 exist** (raw %; 272 items use 138). (8) **Effect value = `EffectBasePoints + EffectDieSides`** (`SpellEntry::CalculateSimpleValue`); item auras use die ∈ {0,1}. (9) **`MOD_SKILL` (30) miscValue = skillId; 305 items = SKILL_DEFENSE (95)**. (10) **6 rare items (incl. 10030 Admiral's Hat) encode MOD_STAT via effect 35 (APPLY_AREA_AURA_PARTY) — dead in this world** (engine target condition never fires for item spells) → floor-only score is the honest value. (11) **`ItemPrototype::Name1` is `std::string`** — `%s` in printf logs is UB (7 playerbot log sites fixed via `.c_str()`).
- **Score-floor fix (calculator):** the v1 `ilvl×10` fallback REPLACED the stat score when `weight_<=0` — at high ilvl it drowned real stat scores (a +9 INT hat would score 18 vs 490 for a statless same-ilvl hat) and degenerated the calculator back to ilvl ranking. Now a small floor: `max(weight_, 1 + q×0.5 + ilvl×0.05)`.
- **Collector additions from the aura-distribution analysis (trigger-1 armor auras):** haste (138/140/141), school-crit (71), percent-only dodge/parry/block (47/49/51), dual melee+ranged routing (52/54), exact effect value (base+die). Coverage: 13 (SP, casters), 22 (resist→armor proxy), 29, 30, 47/49/51, 52/54/55/57/71, 85 (mana regen), 99/124 (AP), 115/135 (healing), 138/140/141, 158 — all major categories; 42 (procs) + 107 (ADD_FLAT_MODIFIER, 64 items) deferred to P2.
- **Verification hook (`PlayerBot.DebugScoreDump`, now OFF):** first 5 bots dump bag items (instance suffix path) + 5 reference items at login (info.log `playerbots: scores: ...`). Verified live 2026-09-18: names render; warrior lvl-14: 1H sword 46.6 vs the SAME sword 2.5 for a caster (DPS weight); +16 AP gauntlets 22.6 warrior / 0.0 rogue (mail-type gate — engine-consistent); +49 STR flask 19.7 (0.15× on-use); broken hat 4.5 = floor-only (negative control). 300/300 online, 0 crashes.
- **`tw` CLI gap found:** DBC store catalog is missing the `Spell` store (`tw dbc Spell` → "unknown store"; parser covers ~54 stores; `DBCfmt.h` uses `const char Xxxfmt[]` arrays). SQL `spell_template` is the canonical spell-query path for now; tw parser fix = backlog.

**Deferred (explicit):** AH integration (R4c), disenchant/repair/professions (no professions on bots yet), real enchant purchase/apply (no enchanters), gem/socket scoring (no data), group need/greed/pass decisions (R4 groups), best-suffix optimism (vanilla instances roll — mean is the right model).

### R4 — Shyalya feature lifts (each = AC-style rewrite)
- **R4a Travel/taxi — MOSTLY SUPERSEDED BY R5e (2026-09-18):** population-level movement (random town travel + homebind refresh) is DONE in R5e as the depth-behavior layer. Remaining R4a = the WALKING layer: port the upstream `TravelSystem-pr` branch (TravelNode graph, prepath + spline reduction, MovementActions +608, `debug zone showpath`) so bots physically walk between zones instead of teleporting; route persistence; real flight-path purchase/taxi usage (bots currently teleport to flight hubs rather than buying/using flight paths).
- **R4b BG tactics:** Arathi Basin node-capture state machine (real banner entries, cast throttle on click), then WSG/AV. `modules/mod-playerbots/src/playerbot/strategy/actions/BattleGroundTactics*.cpp` is the reference.
- **R4c AHBot:** auction posting bot with market policy + profession item supply (CMaNGOS-style, commands included). Requires AuctionHouseHandler core hooks — coordinate with R1 base sync.
- **R4d Scale & scheduling:** ManTech scheduler patterns, continent-spread bot scheduling, bounded maintenance. Target: 1,000 bots on current hardware as first milestone (6k aspirational).

### R5 — Depth behaviors
- **STATUS (2026-09-18):** world population (300 bots, 1-60) + gear/skills + XP-primary targeting + tap-claim contention + relocation self-heal + **real travel system (towns, 1-5h cadence, verified live)** all deployed and stable. Remaining: band-edge level gaps (band-10 KM Venture Co case), per-cell saturation (→ R4d scale work), quest pipeline (accept/track/turn-in — after R4 groups; `killEntry` already cached in the POI cache), walking travel (R4a).
- **World population: level 1-60, all classes, spread across the world (2026-09-17, DONE):** `PlayerbotFactory` now creates a diverse population instead of 100 level-10 warriors-at-heart. Changes:
  - `PickRandomLevel()` returns a **weighted 1-60** level (exponent 1.6 low-level bias; `ceil` keeps 60 as a real top bucket) — was hardcoded 10.
  - New `PickSpawnPosition(level, race)` places each bot in a **level/faction-appropriate, HUMANOID-dense zone**: Alliance→Eastern Kingdoms (map 0), Horde→Kalimdor (map 1). **Spawn coords iterated 3× against `tw_world.creature` (2026-09-17):** (1) first-pass *guessed* coords landed in mob-free areas for 5 of 11 (0 spawns within 700u — my N/S + E/W sign guesses were wrong); (2) replaced with centroids of dense mixed (beast+humanoid) clusters — but that left some bots in **beast-only zones** (`SelectNearestSafeTarget` logged `humanoids=0` → 0 gold, since beasts drop no gold/gear); (3) **final: each band/continent uses a REAL HUMANOID (creature type 7) spawn position in the densest level-appropriate humanoid cluster** (humanoids drop gold + equipment; type 1 beasts drop neither). A real creature position (not centroid) guarantees on-land. Set via `SetLocationMapId`/`Relocate`/`SetMap` (mirrors `Player::Create`), guarded by `MapManager::IsValidMapCoord`.
  - **Scale 100 → 300** bots (`FactoryBotCount`/`MinBots`/`MaxBots`). Headroom: 24 cores (100 bots ≈ 1.8 cores), 17GB RAM free. Regenerated via `DeleteAllBots=1` (wipe+shutdown) then `DeleteAllBots=0` + `FactoryBotCount=300` (recreate).
  - Spells for high-level bots are handled by the existing `AutoLearnSpellsForLevel()` (called at `OnPlayerLogin`) which learns all class spells ≤ bot level from `skill_line_ability`.
  - **Verified:** 300/300 online, 0 crashes, all alive, actively grinding. Distribution: 1-10=98, 11-20=44, 21-30=49, 31-40=36, 41-50=38, 51-60=35. All 9 classes present (Warrior 57, Mage 60, Hunter 45, Rogue 46, Priest 28, Warlock 27, Druid 14, Paladin 12, Shaman 11). Both continents (EK 173 / KM 127).
  - **Target-contention fix (2026-09-17):** with 300 bots concentrating at the dense cluster centers, `SelectNearestSafeTarget` filtered *every* candidate as tapped/attacked-by-another-bot → `best=none` for all (0 kills, 0 gold). Root cause: the contention filter (skip creatures with `GetVictim()!="bot"` or `HasLootRecipient()&&!IsTappedBy`) is correct in principle but with 300 bots in a 50yd sight radius nearly all nearby mobs are contested. Two levers: (1) **`PlayerBot.SightDistance` 50→150yd** — bots now see uncontested mobs out to 150yd and walk to them, spreading the population across the zone; (2) **spawn jitter (±400u random offset)** — bots start spread over a ~1600u-diameter area instead of the exact cluster center. Both need wipe+recreate to take effect (jitter only affects new chars). **Bug fixed (2026-09-17):** the first jitter impl used `(float)(urand(0,800) - 400)` — but `urand()` returns **unsigned `uint32`**, so `urand(0,800) - 400` is unsigned subtraction that **underflows to ~2³²** when `urand<400` → `Relocate(x=4.29e9)` → `IsValidMapCoord` throws `std::runtime_error` → `std::terminate` (crash on startup, mid-`GenerateBots`). Fixed by casting to **signed `int` before subtracting**, plus a `MapManager::IsValidMapCoord(sp.map,x,y,z)` guard in the spawn block that falls back to a safe point (never crashes on a bad coord). **Lesson:** always cast `urand`/`urandRange` results to `int`/`float` *before* any subtraction/signed math.
  - **Level-appropriate gear at creation (2026-09-17, DONE — fixes the "starter junk" blocker):** live inspection showed ALL 300 bots (any level) wielded level-1-2 class starter gear ("Worn Dagger" ilvl 2, Footpad's Shirt/Pants lvl 1) — the old `GetGearSet` was a placeholder and bots literally could not fight mobs above ~level 15. **`ApplyGear` rewritten as runtime gear generation from `item_template` (AC pattern):** one pass over `sObjectMgr.GetItemPrototypeMap()`; per item filter `AllowableClass & classBit` (`1<<(class-1)` = `GetClassMask()` convention; most items are -1/all), `RequiredLevel ∈ [max(1,level-20), level]`, `ItemLevel ≤ level+10` (prevents over-leveled items like "Fast Test Dagger" ilvl-60 at level 5), no `RequiredSkill`/`RequiredCityRank`/`RequiredReputationFaction`; score = `ItemLevel*10+Quality`; keep the **top-16 candidates per equipment slot** (INVTYPE→slot via `PlayerbotInvTypeToSlot`); at equip time try candidates **best-first through the engine's own `CanEquipItem(slot, dest, proto, nullptr, swap=true)`** so class/proficiency/2H-conflict rejections fall through to the next-best (this fixed 93 bots stuck on starter weapons when the single best was class-unusable); **destroy the class starter junk in the slot first** (`GetItemByPos(INVENTORY_SLOT_BAG_0, slot)` + `DestroyItem`) because `FindEquipSlot` only accepts an occupied slot when `swap=true` and `Player::Create`'s starting-items loop fills body/legs/mainhand/ranged with level-1 gear before the factory runs. **Verified after wipe+recreate:** every bot has a mainhand weapon (0 empty), avg 14.2 equipped items, weapon+chest ilvl tracks level by band (1-9→8/10, 20s→30/29, 40s→50/51, 50s→62/60, 60→70); only level-1 bots appropriately keep ilvl-2 starters.
  - **Weapon + defense skills maxed for level (2026-09-17, DONE):** `TrainClassSkills` already grants all classic-valid weapon/armor skills per class (DBC-verified against `SkillRaceClassInfo.dbc`; per-class unions match classic rules — the DBC's odd extra rows like priest-bows are tortoise artifacts and `CanUseItem` gates actual equips anyway), now also grants **`SKILL_DEFENSE` (95) for every class** (was missing entirely → 0 defense skill → extra crits taken) and the cap formula now matches the core's `GetSkillMaxForLevel()` = **`min(level*5, 300)`** exactly (old code's "min 75 for levels 1-9" disagreed with the core, which just clamps at login). **`AlwaysMaxSkillForLevel = 1`** in mangosd.conf → core's `Player::UpdateSkillsForLevel()` (called from `GiveLevel` and the login stats path) re-maxes every level-dependent skill on **login and every level-up**, so bots never fall behind as they grind. **Verified after wipe+recreate:** all weapon skills + defense = exactly `5*level` (lvl 5→25, lvl 20→100, lvl 55→275), gear intact, 300/300 online, 0 crashes.
  - **Sewer Beast lesson — city NPCs are not grind targets (2026-09-17, user-caught):** live logs showed `best=Sewer Beast` (elite 50, entry 3581, spawns at map0 -8784,487 — **inside the Stormwind canals**) picked by a clump of ~30 level 1-57 Alliance bots stuck at the city gate (-8882,565) in a perpetual attack→die→corpse-run loop. Root causes: (1) **every humanoid inside a major city is friendly** → filtered by the unfriendly check → `humanoids=0` → the elite croc was the only "attackable" creature in sight; (2) **no level-difference guard** — `IsHonorOrXPTarget` passes any higher-level mob, so a level-1 bot happily targets an elite 50; (3) **spawn-point selection counted friendly city NPCs as humanoids** (Elwynn band-1 point sat 700u from the SW gate and looked humanoid-rich because of Stormwind citizens). Fixes (all deployed + verified): **`PlayerBot.MaxTargetLevelDiff` (default 7)** — never grind targets more than +7 levels above the bot; **elite/rare/worldboss skip** (`rank > CREATURE_ELITE_NORMAL`) — ungrouped bots never grind elites; **`PlayerBot.HumanoidPreferRange` default 30→150** (full sight); **spawn table v2** (see below). Verified after wipe+recreate: 0 Sewer-Beast targeting, 300/300 online, humanoid picks (`Mudpaw Miner`, `Deepmurk Darkhunter`).
  - **Spawn table v2 (2026-09-17):** re-selected every band/continent point with hostility+level-matched criteria: hostile-only (faction blacklist 12,29,55,68,80,104,35,371,1682 + name eyeball), non-elite (`rank=0`), `level_min ∈ [band_low, band_high-4]` (the v1 spots had +8..+14 mobs for the low half of their bands: Razorfen 33-34 at KM-20, Muckshell 39-43 at KM-30, Spitelash 51-52 at KM-40, Venture 9-10 at KM-1), dense (n≥8 per 150u cell), real creature row for the coord (guaranteed on land). New table: 50 = EK Hearthglen Scarlets 54-57 (n=53) / KM Lucid Dream 55 (n=42); 40 = Bloodsail pirates 43 / Southsea pirates 44-45 (both gold-heavy); 30 = Bloodscalp trolls 34 / Burning Blade 31-33; 20 = Shadowhide 24-25 / Windshear 21; 10 = EK Westfall Defias 14-16 (kept) / KM Venture Co. 14; 1 = EK Northshire kobolds 1-3 (n=50, moved away from the SW gate) / KM Durotar 5.
  - **Loot-money path verified end-to-end; kill distribution is the economy bottleneck (2026-09-17):** the full money chain is CORRECT — `StoreLootAction` (`loot->gold > 0` → `HandleLootMoneyOpcode` → `Player::LootMoney`) both credits (loot.log: `Reden gets 0g0s10c`) and persists (DB `money=17` after save). Item looting works too (jerky/murloc fins in bags). BUT the current kill distribution is ~90% zero-gold creatures: Prairie Wolf/Thalassian Stag/Boar (type 1 beasts, gold 0-0) and the **Mudpaw family — type-7 "humanoids" with gold_min=gold_max=0 (tortoise-custom)**, which breaks the "humanoid ⇒ gold" heuristic. Day-1 totals after wipe+recreate: 116 item loots vs 2 money credits (~30 copper).
  - **R4 direction set (2026-09-17, user):** players using bots to clear **dungeons/raids** will be a significant part of the bot value — group functionality is a first-class priority, not an afterthought. Current chassis already carries the AC group hooks: `StoreLootAction` computes group `PermissionTypes` (master/round-robin/need-before-greed), `Creature::IsTappedBy` is group-aware (groupmate's tap = mine, verified in core), `GetTargetingPlayerCount` kept as the group-assist primitive, and target selection now honors groupmate pulls (AC `AttackersValue` clause 2d — a groupmate's fight belongs to us, grouped bots converge/assist). R4 scope (mine Shyalya for the heavy lifting): group formation/invite flow (player-invited bots), leader follow, tank/heal/dps role coordination, threat-aware targeting, dungeon pathing/travel, group buffing, loot rules, resurrect handling, raid composition.
  - **R5c: XP-primary target scoring (2026-09-17, replaces the humanoid/gold two-tier — user course-correct):** gold awareness was the wrong lever; gold looting is verified working, so target selection is now **primarily XP-based** with soft secondary criteria. Reference-checked both systems: AC `GrindTargetValue::FindTargetForGrinding` = hard filters (XP-eligible, ≤+4 levels, non-elite, z-diff, LOS, lootid-or-hostile) then NEAREST wins — XP is a filter, not a score; Shyalya/ike3 adds soft distance penalties — `!XP::Gain` → 98% skip, not-quest-needed → +20y, contention → +5y/targeting-bot, anti-flap displacement margin, critter skip. Tortoise implementation: hard filters (alive/unfriendly, `IsHonorOrXPTarget` — which already covers gray/totem/pet/**critter**/`xp_multiplier=0`, ≤`MaxTargetLevelDiff`, non-elite, **z-diff > 30y** — both refs have it, prevents cliff-chasing) + **tap-claim contention model (user-directed, AC `AttackersValue` pattern)**: a creature is claimed iff TAPPED (`HasLootRecipient` — set by first damage; tapped-by-other → hard skip, tapped-by-me → valid; group-aware since `IsTappedBy` covers the tapper's whole group); untapped + victim is a player outside my group (pull in progress) → skip; **everything else untapped is fair game regardless of proximity or how many bots are near/targeting it** — dropped the targeting-count contention penalty entirely (was odd: inferred claims from proximity). Scoring: `effDist = dist + max(0, botLevel − mobLevel) × XPLevelPenalty` — below-level (low-XP) mobs look farther, at/above-level mobs unpenalized (both refs treat level only as a cap — no death-risk amplification). Config: `PlayerBot.XPLevelPenalty` (default 4.0 y/level). Removed: humanoid two-tier + `HumanoidPreferRange` + `HumanoidGrindDistanceWeight` (heuristic falsified by 0-gold Mudpaw "humanoids") + contention penalty (usage of `GetTargetingPlayerCount`). Economy instead flows from R3 (vendor/sell loot) once gold-bearing kills accumulate naturally. **R5c follow-ups:** monitor live picks (want: near-level targets, XP accruing, no steal-churn); gold flows once kills distribute to gold-bearing mobs naturally; R3 vendor/sell to convert loot to gold.

  - **R5e: real travel system — user course-correction (2026-09-17):** the relocation sweep was a *diagnostic band-aid* (it proved the mechanics: TeleportTo, ground-z queries, engine/context clearing, idle clocks) — NOT the travel design. The real mechanism (AC reference-mined): population bots get **periodic random travel every 1–5h** — AC `RandomPlayerbotMgr` schedules a per-bot "teleport" event at `urand(min=1h, max=5h)`; on fire: pick a **level-appropriate hub** (real innkeeper/flightmaster/banker NPC positions), 25% chance to a city/banker which also **updates the homebind** (`SetHomebind`), guards (combat/teleporting/BG/arena/group/queued), anti-cam (skip when a real player is within 150y so no visible blink), ground-z via `Map::GetHeight` (AC does exactly `z = 0.05 + ground`), then `MotionMaster::Clear` + `TeleportTo`. Hub list = creature table filtered by `npc_flags & (FLIGHTMASTER|INNKEEPER|BANKER)` (our core: 8|128|256); level match by NPC's own level (town-level proxy: 10=Elwynn ... 30=mid towns, 55+=cities/high hubs — verified live: Tiriel Brightwater lvl10, Brundah Cliffbrow lvl20, 48× lvl30, 81× lvl55); faction via `faction_template.hostile_mask` (bit 4=horde-hostile, bit 2=alliance-hostile — verified empirically). **The relocation sweep stays as the emergency net** (300s grace, grind-spot landings): the two systems compose — travel moves the population + refreshes homebinds; relocation keeps bots productive when they land in a gray inn zone. Quest-driven travel (Shyalya newRPG: accept quest → move to POI → complete; AC `src/Ai/World/Rpg/`) is the NEXT phase (R6), separate from this population-movement layer. newRPG also gates AC's hub selection (hubsPerLevel cache) — ours is the DB-derived equivalent. **Ordering (2026-09-18):** quest loops come AFTER R4 groups (group formation/roles/dungeon pathing first — groupmate pulls are already fair game under the tap model), then quest accept/track/turn-in on top of the travel layer.

  - **R5e POI/quest cache + TravelSystem-pr discovery (2026-09-17):** user directed: cache quest givers and objectives for travel/POI lookup (efficient startup cache, not per-tick DB). **POI data is already in tw_world** — pfQuest-turtle import NOT needed (it's a client-side pfQuest DB extension; the server's quest data is self-contained):
    - `quest_template` (6708): old mangos format — objectives INLINE (`ReqCreatureOrGOId1-4` = kill/turn-in creatures, `ReqSourceId1-4` = items, `ReqSpellCast1-4`), `MinLevel`/`MaxLevel` (0 = uncapped), `ZoneOrSort`, `PointMapId/PointX/PointY` (all 0 in this data — no quest POI markers)
    - `creature_questrelation` (6103 rows, `id`=NPC entry, `quest`): the NPC↔quest link — givers + turn-in NPCs (2163 distinct overworld NPCs on maps 0/1)
    - `points_of_interest` (275): the server's own named POI markers (ObjectMgr.cpp:6600 loads it; `entry`=creature entry, x/y, icon, icon_name e.g. "Lion's Pride Inn", "Zaldimar Wefhellt") — no map column (derive via creature join)
    - `taxi_nodes` (122: id, map_id, x, y, z, name): flight nodes — alt to flightmaster-hub travel
    - `gossip_menu_option` (`npc_option_npcflag` 16/128 = quest offer/reward; `box_coded`=quest): the giver-quest chain
    - **Design:** `PlayerTravelMgr` loads a one-time `QuestPoi` cache (creature_questrelation ⋈ creature ⋈ quest_template, overworld maps 0/1, ~2k rows): `{map, x, y, z, entry, quest, minLevel, maxLevel, killEntry}`. Travel dest pick: 25% city (homebind, lvl 10+), else 50/50 quest-POI vs inn/flight/bank hub (both level-matched; quest MinLevel is a better zone-level proxy than the inn NPC's level). killEntry (=ReqCreatureOrGOId1) cached for the R6 quest phase.
    - **TravelSystem-pr branch (mod-playerbots/mod-playerbots, upstream):** 18 commits ahead of master — WALKING travel system: `TravelNode` graph (prepath + spline reduction), `TravelMgr` rework (+359/-308), MovementActions +608, `.playerbots debug zone showpath` (visualizes travel nodes/walk paths). That's the long-term "bots actually walk to POIs" design (R6+ reference); the random-teleport layer (this commit) is the population baseline. Port-per-A5 later, not now.
    - **R5e night-verify + fixes (2026-09-17/18, deployed 00:31):** verified live with a fast test window (600-1800s): 559 travels in 8h, 237/300 distinct bots, caches loaded (2187 quest NPCs / 213 hubs), 24 city travels with homebind. **Regression found:** the original travel tolerances (hub -10/+5, POI ±3) landed bots in zones whose ambient mobs were +5..+10 — gray under MaxTargetLevelDiff=4 — so a subset of the population ping-ponged: travel→town→stuck (combat with unkillable city/faction mobs or 300s-idle loops)→relocation→travel again (test window made it 10-30min cycles). Kill rate collapsed (430/hr → ~6/hr at nadir). Fixes: (a) **tightened travel matching** — hub [h.level-5, h.level+3], POI ±2, city still 40+; (b) **stale-combat breaker** (`PlayerBot.StaleCombatSeconds=600`): combat streak >10min → drop target (shared context) + grind-spot teleport — bots stuck on city guards previously blocked BOTH the relocation sweep (IsInCombat guard) and travel; (c) production travel window restored (3600-18000s).
    - **R5e.2 — mob-anchored relocation RETIRED (user direction):** the emergency relocation sweep + stale-combat breaker used to land bots next to a hostile mob's spawn (a z/LOS hack). User course-correction: all bot movement should be travel to TOWNS (hub/city/inn/flight/quest POI) — never to a mob. `PlayerTravelMgr::PickDestination` (the 25% city / 50% quest-POI / rest hub mix) is now shared by `DoTravel` AND the emergency sweeps; mob-anchored landing was what dropped lvl-12 bots into Venture Co lvl-14-17 camps tonight. Band anchor + GetHeight survives only as last-resort fallback when no level-matched town exists (band gap). Stale-combat relocations pass `allowCity=false` (don't dump a stuck-in-city bot into another city). The `PlayerbotFactory` grind-spot cache stays in the code but is no longer used by relocation (spawn-band creation still uses `PickSpawnPosition`).
    - **Remaining known issue (pre-existing, not travel):** band-10 KM anchor (1035,-3088) serves lvl-1-9 bots but the dense cluster there is Venture Co. camps (lvl 14-17) → the band-edge level gap for young bots in that band (R4d: per-band spot quality + band-edge spawn table fix).
    - **R5e.2 verification (2026-09-18 AM):** town-landings verified level-matched post-fix (lvl 10→POI min 10, lvl 6→POI min 4, lvl 37→POI min 38, lvl 11→Elwynn hub). **BUG FOUND: uninitialized struct fields** — `EnsureQuestPoisLoaded` set `p.minLevel`/`p.maxLevel` only on the DEDUPE-MERGE branch; the first row per (entry,map) was pushed with stack garbage (often 0 = "uncapped") → any bot level matched any POI (lvl-6 bot at a lvl-50 quest giver). Lesson: every struct field must be assigned before `push_back` — the dedupe-merge pattern hides it because the merge branch looks complete. **Additional matching refinements:** (a) hub pool excludes level ≤5 (wilderness caravan/mount NPCs like Caravan Kodo lvl-1 in lvl-8-12 Westfall); (b) POIs with wide quest span (>15) excluded (mixed-level givers); (c) MaxLevel=0 ("uncapped", common in this data) treated as a minLevel+8 window — otherwise a MinLevel=1 starting-zone POI matched EVERY level.
  - **R5d: graveyard-marooning diagnosis + idle-relocation self-heal (2026-09-17):** after deploying R5c, ~200/300 bots were stacked idle at 6 graveyards (59 at one GY, mixed levels 1-58) — they die somewhere, auto-release, revive at a GY serving a wide level range, and everything nearby is gray → `IsHonorOrXPTarget` filters all → idle forever. The XP filter was CORRECT; the bots were marooned. Fixes: (1) `MaxTargetLevelDiff` 7→4 (AC parity — red +5.. mobs were slaughtering fresh low-level bots, the death source); (2) **idle-relocation sweep** in `PlayerBotMgr::Update` (every 30s): alive, overworld, out-of-combat bots whose grind scans keep failing for `PlayerBot.RelocateIdleSeconds` (180s) teleport to a level-appropriate band spawn (±400 jitter) — world self-heals. Debugging saga (all lessons documented in AGENTS.md): the first version's `e->ai->Reset()` bricked engines (**our `Engine::Reset()` DELETES all strategies/triggers and only `Init()` re-adds them** — never call it standalone; Shyalya's `Reset(true)` doesn't map 1:1); attack actions FAILED every tick on valid targets because **the scan didn't check LOS while `DoAttack` does** (added the AC LOS filter — target behind a cliff = permanent action-fail loop); and bots got **stuck in the COMBAT engine** because our port's `CombatStrategy` deliberately has no retargeting trigger and the only COMBAT→NON_COMBAT exit was `DropTargetAction` — added normalization in `UpdateAI` (server combat flag authoritative; clear the dangling "current target" so "no target" → rescan fires). Post-fix: 300/300 online, kills ~800/hr sustained, money credits + item loots flowing, churn bounded (waiting-out-respawns instead of teleporting every 120s). **Remaining inefficiencies (follow-ups):** band-edge level gaps (level 7-10 bots in 1-3-mob cells), per-cell overcrowding (25 bots/cell vs 2-5 min respawns — multi-cell per band + zone roaming = R4d), death rate (corpse-runners ~15%), and band-1/10 spawns need gold-bearing-cluster review.
- **Target-contention note:** if contention persists even spread out, the deeper fix is R4d (more zones / lower bots-per-zone) — 300 bots is near the density ceiling for the Vanilla mob respawn rate.
- Group play: party formation, role composition, assist/follow semantics (extend existing DpsAssist work).
- Questing: accept/complete cycles (AC quest strategies + bot-buddy command set as reference).
- Dungeons: pathing via mmaps, boss tactics (AC raid scripts as reference).
- Death handling hardening: corpse pathing edge cases, GY resurrection economy.

### R6 — Agent-driven operations (see `agent-bot-interface-plan.md`)
- In-process command API on `PlayerBotAI` (bot-buddy command set).
- Console + HTTP surfaces so pi agent sessions can drive/observe bots.
- Eval harness: scripted scenarios (login→grind→loot→equip→death→revive) asserted via state snapshots.
- This is the force multiplier: agents (GLM/pi sessions) test autonomously while humans review diffs.

### R7 — Quest & RPG layer: FAITHFUL PORT of AC's NewRPG + base quest layer (2026-09-19, course-corrected)

**Mission restated (user directive 2026-09-19):** the goal is a **100% (or as-close-as-possible) port of azerothcore mod-playerbots** into tortoise-wow. No invented behavior; where AC does X, we port X (adapting core APIs, not changing bot behavior). The earlier "teleport-to-POI + 30s mgr-sweep" design was a divergence — **REJECTED**. AC's bots **WALK** to givers, POIs, and towns via the engine's pathfinding; teleport is only a stuck-recovery fallback.

**Overnight (2026-09-19/20, commit 54feac8f — details in AGENTS.md "Overnight work"):** (1) **L6 ACSOAP realtime transport** — AC's `ACSoap` ported (gSOAP HTTP on a dedicated thread, `executeCommand` → `QueueCliCommand` at the authenticated account's real rank → returns the CLI print buffer); vendored gSOAP as its own static lib; **replaces the file-command queue as the preferred realtime transport** (file queue kept as fallback). (2) **GO/chest looting** — `AddAllLootAction` scans GameObjects into the `LootObjectStack` + `OpenLootAction` opens a chest via `SendLoot(goGuid, LOOT_CORPSE)`; **critical fix: the scan must use `Cell::VisitGridObjects`** (the old `WorldTypeMapContainer` visit never called the GO visitor → GO looting was dead); verified live (5 natural GO loots). Test chest `900001` (no-lock) at the level-1 Northshire spawn. (3) **Level-1 regen + broad enablement** — factory `FactoryLevelMin/Max` (default 1/1) → all 300 bots at level 1; `RpgEnabled=1`+`QuestEnabled=1` → every bot walks + quests from level 1. (4) **Idle-relocation sweep REMOVED** (user-directed — invented; AC walks; teleport is stuck-recovery only; the stale-combat breaker is kept). Remaining: locked-chest/lockpick + skill-gated gathering (mining/herbalism/skinning) deferred (level-1 bots lack profession skills); collect/deliver-quest POI resolution pending a data-source decision (pfQuest vs generated creature/GO-loot data).

**How AC actually does it (mined 2026-09-19):**
- **Movement = walking.** `MovementAction::DoMovePoint` = `mm->Clear()` + `mm->MovePoint(0,x,y,z, generatePath=true)` (AC core: `MovePoint(..., FORCED_MOVEMENT_NONE, 0,0, generatePath)`). Our core equivalent: `MotionMaster::MovePoint(0,x,y,z, MOVE_PATHFINDING)` (option flag 0x001) — **same architecture**; the base `MovementAction::MoveTo` in our chassis already does this (used by Follow/MoveRandom).
- **Long-distance walking = `NewRpgBaseAction::MoveFarTo(WorldPosition)`** (NewRpgBaseAction.cpp:40-204): (1) in-flight guard; (2) new dest → `rpgInfo.SetMoveFarTo(dest)` (clears stuck state); (3) `IsWaitingForLastMove` gate (shared "last movement" value + `maxWaitForMove` delay); (4) **committed-movement early-out**: if bot is still walking its last committed point (same map, >10yd remaining) → let the spline finish (prevents mid-walk re-pathing oscillation — AC's hard-won fix); (5) **stuck detection**: distance-improvement tracker (5yd meaningful-improvement rule); no improvement for `stuckTime` with ≥5 attempts → **`TeleportTo(dest)` recovery** (logged, zone name included); (6) `dis < pathFinderDis` → straight `MoveTo`; (7) else `PathGenerator path(bot); path.CalculatePath(dest)` — if `PATHFIND_NORMAL|INCOMPLETE|FARFROMPOLY` and path endpoint makes >5yd progress toward dest → `MoveTo` **the path's actual endpoint** (walk the furthest reachable waypoint); (8) fallback: **cone sampling** (2 samples, ±π/2 forward cone, 0.5-1.0×pathFinderDis) → best (smallest angle delta) reachable stepping stone → MoveTo there.
- **NPC approach = `MoveWorldObjectTo(guid, distance=INTERACTION_DISTANCE)`** (cpp:205-238): `IsWaitingForLastMove` gate → random approach angle (±25% jitter, leads moving targets) → collision-checked coords (`CheckCollisionAndGetValidCoords`, fallback to NPC pos) → `MoveTo`.
- **Local wander = `MoveRandomNear(moveStep=50)`** (cpp:240+): multi-attempt random destination sampling with valid-coordinate checks (single-sample version was rejected upstream: water/blocked/unreachable).
- **RPG state machine = `NewRpgInfo`** (variant over statuses: Idle, GoGrind, GoCamp, WanderNpc, WanderRandom, DoQuest, TravelFlight, Rest, OutdoorPvP) + per-status max durations + weighted random transitions (`NewRpgStatusTransitionProb`), DoQuest fields: `questId, objectiveIdx, pos, lastReachPOI` + MOVE_FAR stuck fields (`moveFarPos, nearestMoveFarDis, stuckTs, stuckAttempts`) + `startT`.
- **Quest data plumbing = base `QuestValues`**: entry→questid relation map (creature_involvedrelation/questrelation/gocreature), quest→giver guid positions, quest→objective positions, `QuestGivers/ActiveQuestGivers/Takers/Objectives` values. **Key finding: these values have NO consumers in the base layer** — the base layer is interaction (talk/accept/turn-in/decline/confirm) + data; **movement/state is newRPG's job**.
- **Quest interaction = CMSG replay** (AC `AcceptQuestAction`/`TurnInQuestAction` build `WorldPacket(CMSG_QUESTGIVER_ACCEPT_QUEST) << guid << questId` → `bot->GetSession()->HandleQuestgiverAcceptQuestOpcode(pkt)`; reward choice = `CHOOSE_REWARD << guid << quest << rewardIdx`; decline = `DECLINE_QUEST`; drop = `QUESTLOG_REMOVE << slot`) — same pattern we need (no WorldPacketTrigger in our core → `PrepareQuestMenu(guid)` polling is the equivalent menu source; AC's newRPG also uses menu polling via `HasQuestToAcceptOrReward`). **PROVEN end-to-end (2026-09-19, commit 352871d6):** the full accept→turnin cycle works via this CMSG-replay pattern (agent file queue, Inisto lvl 59, quest 16): `accept 16 <giver>` (ACCEPT_QUEST→AddQuest) then turnin = **TWO opcodes** — `CMSG_QUESTGIVER_REQUEST_REWARD` (→CompleteQuest+offer) THEN `CMSG_QUESTGIVER_CHOOSE_REWARD` (→RewardQuest+RemoveQuest). **`CMSG_QUESTGIVER_COMPLETE_QUEST` is only the hand-in-items dialog (NO state change) — do NOT use it for turn-in.** L4 `TurnInQuestAction` must replay REQUEST_REWARD+CHOOSE_REWARD, not COMPLETE_QUEST.

**Our core's movement API (verified 2026-09-19):** full recast-navmesh pathfinder: `PathFinder`/`PathInfo` (Maps/PathFinder.h) — `calculate(x,y,z, forceDest, offsets)`, `getPathType()` (same `PATHFIND_*` family: NORMAL/SHORTCUT/INCOMPLETE/NOPATH/NOT_USING_PATH...), `getActualEndPosition()`, `getPath()` (PointsArray), `Length()`; 2.3GB mmaps extracted (`server/data/mmaps/`). `MotionMaster` (TC-lineage): `MovePoint(id,x,y,z, options, speed, finalOrient)` (MoveOptions: `MOVE_PATHFINDING=1, MOVE_WALK_MODE=2, MOVE_RUN_MODE=4, MOVE_FORCE_DESTINATION=0x40...`), `MovePath(PointsArray, mode, flying, walk)` ("a module supplies a precomputed path"), `MoveChase/MoveFollow/MoveJump/MoveCharge/MoveSeekAssistance`, `GetDestination()`, `GetCurrentMovementGeneratorType()`. PathFinder already consumed by core movement generators (Fear/Confused/Home/Targeted/Point).

**Port plan (layers, in order):**
1. **L1 Movement completion** (extend `PlayerBots/Ai/Base/Actions/MovementActions.h/.cpp` + new `LastMovementValue`): port AC's `MovementAction` protected API — `MoveTo(mapId,x,y,z,...)` (cross-map check), `MoveNear`, `MoveToLOS`, `ChaseTo`, `ReachCombatTo`, `WaitForReach`, `MoveDelay(distance)` (distance-proportional delay, cap `maxWaitForMove`), `IsWaitingForLastMove/SetNextMovementDelay` (shared "last movement" context value: lastMoveToMapId/X/Y/Z + delay + priority), `IsMovingAllowed` (channel/aura state), `SearchForBestPath` (z-modification retry loop), `MoveInside`, improved `MoveRandomNear` (multi-attempt). New actions (ported): **`MoveFarToAction`** (AC algorithm verbatim, `PathGenerator`→`PathFinder`, `WorldPosition`→(map,x,y,z) struct), **`MoveWorldObjectToAction`**, **`MoveRandomNearAction`**. Teleport ONLY as stuck-recovery (stuckTime default = AC's, ~120s; config).
2. **L2 RPG state machine** (port `NewRpgInfo` → `PlayerRpgInfo` on PlayerbotAI; vanilla-relevant statuses only: IDLE/GRIND/WANDER/QUEST/REST — GoCamp/WanderNpc/TravelFlight/OutdoorPvP are WotLK-content): DoQuest fields (questId, objectiveIdx, poi map/x/y/z, lastReachPOI), MOVE_FAR stuck fields, `startT`, weighted status transitions with per-status max durations (port AC's transition-prob table, vanilla subset).
3. **L3 Quest data** (keep the startup SQL cache already written in `PlayerQuestMgr::Load()` — givers/takers/objectives/POIs; equivalent to AC's GuidPosition maps; documented adaptation: SQL cache vs AC's runtime spawn containers). Expose via values: quest relation map, quest givers/takers/objectives, "possible new rpg targets".
4. **L4 Quest actions** (port base-layer actions, CMSG replay): `TalkToQuestGiverAction` (MoveWorldObjectTo + PrepareQuestMenu + dialog state), `AcceptQuestAction` (menu scan → best: level ±2, Type=0, SuggestedPlayers<2, !repeatable, CanTakeQuest+CanAddQuest → ACCEPT replay → CONFIRM), `TurnInQuestAction` (menu scan → complete → `BestRewardIndex` via StatsWeightCalculator → CHOOSE_REWARD replay), `DropQuestAction` (organize/abandon → QUESTLOG_REMOVE), `DeclineQuestAction` (log overflow).
5. **L5 Wiring** (port NewRpgStrategy): NON_COMBAT trigger "rpg state" → "change rpg status" action (weighted pick); "do quest" action = the state machine (find giver → **walk** (MoveFarTo/MoveWorldObjectTo) → accept → walk to POI → existing GrindingStrategy does kills (tap model) → progress check (QuestStatusData.m_creatureOrGOcount) → walk to taker → turn in → next).

**Interactions with existing systems (keep — they ARE the AC equivalents):** R5e 1-5h town travel = AC RandomPlayerbotMgr population-bot behavior (kept; skipped while quest status active); idle-relocation + stale-combat breaker = emergency safety nets (skip quest-active bots); XP-primary tap-claim grinding = our grind layer (AC-equivalent); R3a item scoring = reward choice + equip.

**Documented adaptations (API-level only, behavior identical):** (1) `PathGenerator`→`PathFinder`, `WorldPosition`→(map,x,y,z), `MovePoint(generatePath)`→`MovePoint(MOVE_PATHFINDING)`; (2) no WorldPacketTrigger → `PrepareQuestMenu` polling (AC newRPG does the same); (3) no QuestPOI.dbc (vanilla) → POI from spawn tables (exact mob clusters — arguably better); (4) quest data SQL startup cache vs runtime containers; (5) WotLK statuses/raids omitted in v1 (vanilla dungeons/raids = R4); (6) `PATHFIND_FARFROMPOLY` doesn't exist in our PathType (use NORMAL|INCOMPLETE|SHORTCUT... as typeOk); (7) `CheckCollisionAndGetValidCoords` → core equivalent (`sMapMgr.GetHeight` guard / `Map::CheckCollision` if present — verify at impl).

**Rejection record:** `PlayerQuestMgr::ProcessBot()` 30s-sweep + teleport-to-POI design (written 2026-09-18) is **superseded** — its startup cache is kept (L3); its sweep/teleport logic is replaced by the AI-layer state machine (L2/L4/L5) with walking (L1). `BotQuestState` (per-bot state in mgr) → `PlayerRpgInfo` (on the AI, AC pattern).

**L1 status (2026-09-19, DONE + deployed):** `MovementActions.h/.cpp` rewritten with the full AC MovementAction protected API — `MoveTo(mapId,x,y,z,...)` (cross-map guard, exact_waypoint/no-path branch, SearchForBestPath z-modification, LastMovement commit+delay), `MoveNear`, `MoveDelay` (speed-based: swim/run(flight uses run in 1.18)/run-back), `WaitForReach` (globalCoolDown cap under combat), `SetNextMovementDelay` (MOVEMENT_FORCED), `IsWaitingForLastMove` (priority + delay window), `IsDuplicateMove` (lastMoveShort + 0.01f + maxWaitForMove window), `IsMovingAllowed` (CanMove + channel check), `SearchForBestPath` (PathFinder + `Map::GetHeight` z-search ±step, `maxMovementSearchTime`), `DoMovePoint` (`mm->Clear()` + `MovePoint(MOVE_PATHFINDING)`); `MoveFarTo` (AC algorithm verbatim: in-flight guard → SetMoveFarTo → IsWaitingForLastMove → committed-movement early-out (>10yd) → stuck check (5yd rule, ≥5 attempts, moveStuckTime) → `<pathFinderDis` straight → PathFinder-to-true-dest with progress guard → 2-sample forward-cone fallback); `MoveWorldObjectTo` (±25% approach jitter, leads moving targets, `MapManager::IsValidMapCoord` guard); `MoveRandomNear` (8 attempts, 0.4-1.0×step, path+coords+water checks via `GetTerrain()->GetWaterOrGroundLevel`). `LastMovementValue.h` rewritten as AC superset (lastdelayTime/lastMoveShort added to the committed version; taxi/flee/area-trigger fields carried for parity, unused in v1) + new `LastMovementValue.cpp` (AC-verbatim `Set/clear/setShort`); `StayTimeValue` registered as "stay time". `PlayerRpgInfo.h/.cpp` new (vanilla status subset: IDLE/GRIND/WANDER_RANDOM/DO_QUEST/REST + DoQuest fields + MOVE_FAR stuck fields + startT; AC-aligned values). `PlayerBotAI::CanMove()` upgraded to full AC port (lost-control/death/CC/MM-controlled-types/flight/teleport; no vehicle check — none in core). `MoveRandomAction` now delegates to `MoveRandomNear(50, WANDER)` (path-validated wander; group-leader-only isUseful = no-op pre-R4). Config: `PlayerBot.PathFinderDis=70`, `MoveStuckTime=90000`, `MaxMovementSearchTime=3` (AC defaults). **API lessons:** `PATHFIND_FARFROMPOLY` absent → SHORTCUT stands in for typeOk; `rand_norm()` core = `rand_norm_f()` (Util.h); `Unit::IsMoving` (capital I); `Unit::isFrozen` is a DEAD declaration in this core → `IsFrozen()`; `Map::GetHeight` (not GetMapHeight); **`Player::FindQuestSlot`/`GetQuestSlotQuestId` made PUBLIC** (Player.h, AC API parity — the AC module calls them 7×; pure access-level change, no behavior). **Rejected-sweep state:** `PlayerQuestMgr::ProcessBot`/`TeleportToPoi` compile but are DORMANT (`PlayerBot.QuestEnabled=0` default; its 30s sweep + Load() gated) — L3 will make `Load()` unconditional (cache kept) and L4 replaces the sweep. L1 verified live 2026-09-19: 300/300 online, 299 alive (1 death cycle), 0 crashes 15min, sweeps/travel/equips still firing. **L1b (combat movement actions: Flee/MoveInside/AvoidAoe/RearFlank/MoveToLOS/ChaseTo/ReachCombatTo) deferred** — combat currently uses `MoveChase` (pre-existing, works).

**L2 status (2026-09-19, DONE + verified live):** the proper movement layer that replaces the teleport band-aid. Bots **WALK** to grind spots (L1 pathfinding), wander locally, and rest — driven by the AC `NewRpgInfo` weighted-random state machine. **New files:** `Util/PlayerGrindMgr.h/.cpp` (level-band grind-position cache — the vanilla equivalent of AC's `sTravelMgr.GetLocsPerLevelCache`, WotLK-only: startup SQL `creature` ⋈ `creature_template` [rank=0, level 1-60, maps 0/1, ~63k spawns] grid-cell-sampled at 1500yd cell × 5-level band to a small well-spread cache; **verified live: 63,421 raw spawns → 472 (map0) + 595 (map1) = 1,067 spots**). `SelectRandomGrindPos` = same map + level band ±3 + within 2500yd (lo)/<500yd (hi), 50% hi else lo — **AC's pick verbatim (NewRpgBaseAction.cpp:967-968 hiRange=500/loRange=2500, :988 >2500 skip, :1006 50% hi)** — + `sMapMgr.FindMap(mapId)->GetHeight` ground-z. `Ai/World/Rpg/RpgBaseAction.h/.cpp` (`RpgBaseAction : MovementAction` — the state-machine helpers from AC `NewRpgBaseAction`: `RandomChangeStatus` = weighted-random pick from available [weight>0 && CheckRpgStatusAvailable] + per-status setup (GO_GRIND picks a spot, REST sits), float roll (not AC's `urand(1,probSum)` truncation bug) + `CheckRpgStatusAvailable` [WANDER needs a nearby "grind target", GO_GRIND needs a grind spot, IDLE/REST always, DO_QUEST deferred]; the far-movement helpers live on the L1 `MovementAction` base — we split movement=L1 / state-machine=L2). `RpgActions.h/.cpp` (`RpgStatusUpdateAction` = the driver, default action @11.0 [AC "relevance > grind"]: IDLE→RandomChangeStatus, GO_GRIND→WANDER on arrival <10yd, WANDER/REST→IDLE on `HasStatusPersisted(maxDur*1000)`; `RpgGoGrindAction` = `MoveFarTo(bot->GetMapId(), grindX/Y/Z)` [WALK] + `MoveRandomNear(10.0f)` nudge fallback [AC verbatim]; `RpgWanderRandomAction` = `MoveRandomNear()`). `RpgStrategy.h/.cpp` ("rpg": default "rpg status update"@11.0; triggers "go grind status"→"rpg go grind"@3.0, "wander random status"→"rpg wander random"@3.0; REST/IDLE need no movement action). `RpgTriggers.h/.cpp` (`RpgStatusTrigger`, one parameterized trigger per status — AC `NewRpgStatusTrigger`). **Config:** `rpgStatusProbWeight[]` (GO_GRIND 50 / WANDER 30 / REST 10 / IDLE 0 / DO_QUEST 0), `rpgWanderRandomStatusMaxDuration=300`/`rpgRestStatusMaxDuration=30`/`rpgDoQuestStatusMaxDuration=1800` (config in SECONDS; AC `5*MINUTE*IN_MILLISECONDS` etc. → ×1000 at the `HasStatusPersisted` call), `rpgEnabled=false` (default OFF — existing Wander/Grind/Loot stay the default; per-bot `adds rpg` via R6.1c). **Wiring:** `RpgStrategy` → NON_COMBAT at login when `rpgEnabled=1`; all actions/triggers/strategy registered in the contexts; `RPG_MAX_STATUS=8` added to the `PlayerRpgStatus` enum; `PlayerbotAIConfig.h` now includes `PlayerRpgInfo.h` (for the enum — note: forced a broad rebuild once). **Composes with existing strategies (the AC pattern):** Rpg="where to be" (walk to a grind spot + mill around it); the existing GrindingStrategy="what to attack" (nearest safe target). On arrival the bot wanders while the base grind strategy kills the mobs there; GO_GRIND walking (up to 2500yd) uses L1 `MoveFarTo` pathfinding and the bot grinds any killable mob it meets on the way (combat engine takes over, then resumes walking). **DO_QUEST deferred to L3+** (needs the quest POI pipeline; WotLK statuses omitted). **Verified live 2026-09-19 (10-bot soak):** 300/300 online, 0 crashes; grind cache loads (1,067 spots); bots **WALK** (not teleport) — smooth multi-hundred-yd pathfinding movement (Deres/Inisto/Appolonia moved 240-1700yd over 3-8 min); state machine **cycles** (info.log `playerbots: rpg <bot> -> rest/go_grind` transitions, death→respawn→re-pick confirmed); bots **grind** (enter combat with mobs mid-walk); **stuck-recovery teleport fires** when a spot isn't walkable-reachable (MoveFarTo stuck detection → `TeleportTo` — the L1 designed fallback, observed via the teleport-ACK logs). **Known dynamics (AC's actual behavior, not bugs):** (a) GO_GRIND walks are up to 2500yd (AC's exact distance) so bots can die or get stuck-in-combat before reaching the spot → the `go_grind→wander` (arrival) transition is logically correct (AC's `dis<10`) but not always observed for a bot that dies mid-walk; the world's baseline death rate is ~14% (spans all levels, non-Rpg bots included) so Rpg isn't an abnormal spike; (b) the Rpg (NON_COMBAT) pauses while a bot is in the COMBAT/DEAD engine (expected — the state machine is a non-combat concern). **Broad rollout (`rpgEnabled=1`) is a SEPARATE decision** — default OFF so the 300-bot world is unchanged on restart; enable per-bot via `adds rpg` (verified) or flip `rpgEnabled=1` for the whole world (pending). **Pitfalls (this core):** `Action::Execute(Event)` BY VALUE (not `Event&`); `QueryResult` is in `Database/DatabaseEnv.h` (no `QueryResult.h`); `sMapMgr` has NO `GetHeight` → `sMapMgr.FindMap(mapId)->GetHeight(x,y,z)`; `AI_VALUE` macro needs `#include "AiObjectContext.h"` (complete type); config durations in SECONDS → ×1000 for the ms `HasStatusPersisted`.

**L3 status (2026-09-19, DONE + verified live):** the quest DATA layer — exposes the `PlayerQuestMgr` startup cache to the AI via 4 context values (AC `QuestValues.h` port, names + checkInterval parity). **Changes:** (1) `PlayerQuestMgr::Load()` made **unconditional** in `PlayerBotMgr::Load` (was gated by `questEnabled`) — the cache is the DATA layer; the quest ACTIONS/sweep stay gated. **Load() had a LATENT crash:** the objectives SQL used `SELECT id AS quest ... FROM quest_template` but **`quest_template`'s PK is `entry`, not `id`** (same lesson as `creature_template`) → errno 1054 → `MySQLConnection::HandleMySQLError` throws → uncaught → `std::terminate()`/SIGABRT on startup. Never ran before (Load was gated off). Fixed `id`→`entry` in all 4 UNION branches. (2) **off-by-one fix:** the objectives subquery generated `objidx` 1-4 but the C++ `Quest::ReqCreatureOrGOId[QUEST_OBJECTIVES_COUNT=4]` / `m_creatureOrGOcount[4]` arrays are 0-3 → changed the SQL to generate 0-3 (matches the struct, no off-by-one at the `GetActiveObjectives` call site). (3) New `Util/PlayerQuestMgr` data accessors: `GetFreeQuestLogSlots(Player const*)` (counts empty slots), `GetActiveObjectives(Player*, vector<QuestDest>&)` (incomplete quests' still-to-do kill-objective POIs, same map+zone+`questPoiMaxDist=1500`, nearest-first), `GetActiveTakers(Player*, ...)` (complete-not-rewarded quests' taker POIs, same filter), `GetNearbyGivers(Player*, ...)` (live cell scan within `questAcceptRadius=80` + `PrepareQuestMenu` + `WorthAccepting` per giver; dungeon-gated). `PoiMatchesZone` = same map + `sTerrainMgr.GetAreaId` equal + within maxDist (AC parity). (4) **New `Ai/Base/Value/QuestValues.h/.cpp`** (registered in `BaseAiObjectContext.cpp`): `FreeQuestLogSlotValue` ("free quest log slots", interval 2), `ActiveQuestGiversValue` ("active quest givers", interval 5, on-demand live scan — NOT config-gated, AC parity), `ActiveQuestTakersValue` ("active quest takers", 5), `ActiveQuestObjectivesValue` ("active quest objectives", 5). Vector values return `std::vector<QuestDest>` (QuestDest = {map,x,y,z,entry}), nearest-first (distance sort). **Verification hook:** `questdata` field added to the agent `state` snapshot (BotStateSnapshot.cpp `AppendQuestData`) — shows freeSlots + givers/takers/objectives counts + nearest POI (calls the same PlayerQuestMgr accessors). **Verified live 2026-09-19:** cache loads clean (300/300 online, 0 crashes): **`quest cache loaded: 5938 taker quests, 555 objective quests, 2163 giver entries (overworld)`**; `questdata` in the `state` snapshot computes — `freeSlots:20` (empty log) and **`givers:1`** for Belleni (lvl 34 Human Priest) at Jennea Cannon in Stormwind (cell scan + `WorthAccepting` correctly found a level/class-compatible giver; quest 1953 is Warlock-only [RequiredClasses=128] so a Priest correctly doesn't match it). takers/objectives are the same code pattern (data populated) and get exercised end-to-end by L4/L5 (bot does a real quest). **Pitfalls (this core):** `quest_template` PK = `entry` (NOT `id`); `creature` PK = `guid` (NOT `id`; `creature.id` IS the entry); `creature_involvedrelation`/`creature_questrelation` cols = `id` (entry) + `quest`; `getQuestStatusMap()` + `PrepareQuestMenu` are NON-CONST → the L3 accessors that call them take `Player*` (not `const*`); `QUEST_OBJECTIVES_COUNT=4` (0-3); a low-level bot at an Elwynn giver DIES to the local mobs before a `state` query (clear-all leaves it defenseless) — use a safe-town (Stormwind) bot for the live givers test.

**L4/L5 status (2026-09-20, DONE + deployed + accept verified live):** the quest ACTIONS + WIRING — the do-quest state machine. **`RpgBaseAction` gains 8 quest helpers (AC `NewRpgBaseAction` port):** `SearchQuestGiverAndAcceptOrReward` (overworld + alive + not-in-combat; cell-scan the nearest "possible rpg target" ≤ `questAcceptRadius` with `HasQuestToAcceptOrReward` → `CanInteractWithQuestGiver` → `InteractWithGiverForQuest`, else `MoveWorldObjectTo`; **called from go_grind/wander/do_quest so bots pick up quests while milling in towns**), `HasQuestToAcceptOrReward` (`PrepareQuestMenu` + scan the menu: any COMPLETE + `CanRewardQuest` → turn-in, any NONE + `WorthAccepting` + `CanTakeQuest` + `CanAddQuest` → accept), `ChooseGiverToInteract` (overworld cell scan of `PlayerQuestMgr::GetGiverEntries()` + same-zone check), `AcceptQuestAtGiver` (**DIRECT `Player::AddQuest` — not CMSG replay — guarded by `CanTakeQuest` + `CanAddQuest`**, erases from `rpgLowPriorityQuest`, auto-`CompleteQuest` if `CanCompleteQuest` immediately), `TurnInQuestAtGiver` (`CompleteQuest` if not COMPLETE → `GetQuestRewardStatus` + `CanRewardQuest(quest,bestIdx)` → `RewardQuest(quest,bestIdx,giver,false)`; `CompleteQuest` auto-rewards on `QUEST_FLAGS_AUTO_REWARDED` so the explicit `RewardQuest` is only called when `CanRewardQuest(quest,bestIdx)` to avoid a double reward), `BestRewardIndex` (`StatsWeightCalculator::CalculateItem` score of `RewChoiceItemId[]`, picks the highest), `SelectDoQuestQuest` (iterate the quest log, skip `rpgLowPriorityQuest`, pick a quest with a valid kill-POI (`GetQuestObjectivePoi`) or taker-POI (`GetQuestTakerPoi`), random pick). **`PlayerQuestMgr` gains:** `GetQuestObjectivePoi(questId, &objIdx)` (nearest incomplete-kill-objective spawn + the objective index), `GetQuestTakerPoi(questId)` (nearest taker POI for a complete quest), `IsWorthAccepting` (public wrapper for the private `WorthAccepting`), `GetGiverEntries()` (static, the cached giver-entry set). **`PlayerBotAI` gains `rpgLowPriorityQuest`** (`std::unordered_set<uint32>`, AC `lowPriorityQuest` parity — quests abandoned for no progress, not re-selected). **`RpgDoQuestAction` (new, registered `"rpg do quest"`):** `SearchQuestGiverAndAcceptOrReward` first (turn-in/accept at a nearby giver); then switch on `GetQuestStatus(questId)`: INCOMPLETE → `DoIncompleteQuest` (objective-done check via `m_creatureOrGOcount[idx]`; if no POI, `GetQuestObjectivePoi` + set `rpgInfo` POI; far >10yd → `MoveFarTo`; on arrival `lastReachPOI` + `MoveRandomNear(8)`; `questNoProgressSeconds` (AC `poiStayTime` 5min) no-kill → `rpgLowPriorityQuest` + abandon), COMPLETE → `DoCompletedQuest` (`GetQuestTakerPoi` → `MoveFarTo`; the turn-in is done by `SearchQuestGiverAndAcceptOrReward` on arrival; `questNoProgressSeconds` → abandon), else → `ChangeToIdle`. **`RpgStrategy`** adds the `"do quest status"` trigger → `NextAction("rpg do quest", 3.0f)` (the `"do quest status"` trigger was already registered in TriggerContext; `RpgStatusUpdateAction` DO_QUEST case already exists). **Config weights → AC defaults:** `rpgStatusProbWeight[DO_QUEST]=60` (dominant — questing is the primary activity when available), GO_GRIND=15, WANDER=15, REST=5, IDLE=0. **POI selection = NEAREST (documented simplification; AC picks a random POI from the list — nearest avoids walking far).** **Pacing (AC `ForceToWait(5000)` parity):** `PlayerRpgInfo::lastQuestInteract` ms timestamp → `SearchQuestGiver` skips re-interaction for 5s after an accept/turn-in (spaces out rapid-fire menu hits without pausing all bot actions). **Verified live (2026-09-20):** build green, 300/300 online, 0 crashes; **AI-driven ACCEPT PROVEN in a natural scenario** — Inisto (lvl-59 Druid) traveled to Darnassus (R5e travel) + the AI accepted 4 level-matched quests (7792/7798/7799/7800 "A Donation of …: Darnassus") via `SearchQuestGiverAndAcceptOrReward` (quest log shows all in_progress; `questdata` freeSlots=16). **NEXT: the full KILL-quest cycle (accept→walk→kill→turn-in)** — the donation quests are item-delivery (no kill POI, so `do_quest` doesn't roll for them); a kill quest is needed to verify the walk-to-POI + `m_creatureOrGOcount` progress + taker turn-in. Blocked by the controlled-test 5-way alignment (level±2 band + class + faction + safe-zone + known objective spawn) for the 10 high-level soak bots (kill-quest givers sit in HOSTILE zones → the bot aggro's + dies before the NON_COMBAT accept fires; faction matters — a Horde bot attacks an Alliance giver). The 300-bot world exercises the kill cycle naturally (bots travel to level-matched towns + accept kill quests + walk to objectives). **Pitfalls (this core):** `QuestDef.h` is the `Quest` class (NOT `Quest.h`); `QUEST_OBJECTIVES_COUNT=4` (0-3) / `QUEST_REWARD_CHOICES_COUNT=6`; `QuestMenu::GetItem` is NON-CONST (bind `QuestMenu&`, not `const&`); `CompleteQuest` auto-rewards on `QUEST_FLAGS_AUTO_REWARDED` (explicit `RewardQuest` only if `CanRewardQuest(quest,bestIdx)`); `InteractWithGiverForQuest` must be declared in RpgBaseAction.h (a .cpp-only definition → undefined at the call site); engine relevance = higher-runs-first (status-update 5.0 before do-quest 3.0 = the timer pattern); `GiverScannerVisitor` is file-scope in PlayerQuestMgr.cpp (RpgBaseAction has its own `RpgGiverScanner`); no `FindNearestCreature` in this core (cell scan directly); `Map::GetId()` lowercase 'd'.

**CRITICAL INFRA FIX (2026-09-18) — headless far-teleport world-drain (was blocking all live L4 quest testing):** ~40% of bots left the world 5-10min after login (DB still 300 online). Root cause: headless (socketless) bots cannot complete a **far (cross-map) teleport** — `ExecuteTeleportFar` removes the bot from its old map and waits for the client's world-port ACK (`HandleMoveWorldportAckOpcode`) to re-add it to the destination map; no client → never re-added → stuck "between maps" (`IsBeingTeleportedFar()=true`, `IsInWorld()=false`). Triggered by the per-login race start-town teleport (CharacterHandler.cpp:954) for any bot whose saved map differs. **Fix:** `HeadlessSessionMgr::Update` now calls `session->HandleMoveWorldportAckOpcode()` (no-arg server-side overload) for any headless player with `IsBeingTeleportedFar()`, completing the teleport on the next tick (out-of-world ≤1 tick). Verified 300/300 stable (was 300→240→138), 0 crashes. Near (same-map) teleports were unaffected (self-complete via the `MOVEMENT_CHANGE_ACK_TIME*5` timeout). General lesson: **any client-ack-gated operation needs a headless self-heal.** Diagnostics kept: far-tp completion log, session-DESTROYED log, OUT-OF-WORLD regression detector, Player-destructor log. (Full detail in AGENTS.md "Headless Far-Teleport World-Drain".)

**Config (ported AC names where they exist):** `PlayerBot.QuestEnabled`, `maxWaitForMove` (AC default 5000ms — already have), `rpgDistance`, `pathFinderDis` (AC default 250?), `stuckTime` (MoveFarTo stuck recovery), `questLowLevelHideDiff`, per-status durations (port AC defaults: statusDoQuestDuration etc.), `QuestNoProgressSeconds=300` (AC `lowPriorityQuest` 5-min rule).

**Verification:** GM session (`.select <bot>`, `.quest add/complete`) + watch info.log: bots visibly WALKING to givers/POIs (movement debug logs, client-visible), `playerbots: ... accepted/turned in quest`, stuck-recovery teleports rare (logged with zone), zero crashes at 300, no relocation-loop regression, quest log fill rate + turn-ins/hour.

## 6. Risks & mitigations

| Risk | Mitigation |
|---|---|
| Shyalya repo archived / history lost | **Mitigated 2026-06-27: full history fetched as remote `shyalya` into `/root/tortoise-wow` (903 commits)** + mirror at `/root/shyalya-tortoise-wow` |
| 251-commit rebase conflicts in core touchpoints | Rebase early (R1), small commits, soak gate before any new features |
| ike3 logic subtly wrong for vanilla 1.18.1 | Cross-check lifts against AC reference + tortoise-data DBC queries; soak tests per lift |
| Scope explosion now that limits are off | Phased exits above; each phase lands on green soak; master plan is the single source of truth |
| LLM/agent-driven testing destabilizes server | R6 interface is read-mostly by default; write commands gated behind config + GM rank |

## 7. Doc map

| Doc | Role |
|---|---|
| `bot-master-plan.md` | This file — strategy, decisions, roadmap |
| `playerbot-port-plan.md` | Historical: engine port detail, phase 0-3 internals (scope-limited) |
| `playerbot-review.md` | Local-AI critical review (2026-06-26) + reconciliation table above |
| `r1-base-sync-log.md` | R1 issues & solutions (merge conflicts, WorldSession plumbing, updater keys, migration reconciler) |
| `tortoise-data-plan.md` | tortoise-native data tool (`tw` CLI) — new build, not an acore-data fork; **live as global pi extension** (`~/.pi/agent/extensions/tortoise-data.ts`, tool `tw`, works from any directory) |
| `agent-bot-interface-plan.md` | bot-buddy logic port — agent↔bot control surface |
| `pi-websearch-extension.md` | **RESOLVED** — web search/fetch via `pi-web-access` package (Exa zero-config); history retained |
| `AGENTS.md` | Hub: server ops, working notes, doc pointers |
